/pb_data/
/pb_data_controller/
/pb_data_timeclock/
/internal/ui/dist/
/kiosk.yaml
/controller.yaml
/timeclock.yaml
# Built binaries — names vary by build command / OS. Cover the documented
# kiosk-app/* names plus the bare `go build ./cmd/<x>` outputs and editor
# backups (kiosk.exe~).
/kiosk-app
/kiosk-app.exe
/kiosk
/kiosk.exe
/kiosk-controller
/kiosk-controller.exe
/controller
/controller.exe
/kiosk-timeclock
/kiosk-timeclock.exe
/timeclock
/timeclock.exe
*.exe~
/ui/node_modules/
/ui/dist/
/ui/.vite/
# Site-specific branding overrides stay local to each deployment; the
# committed reference theme keeps its .example suffix.
/branding/*.css
*.log
.DS_Store
.env
# Local Claude Code session config
/.claude/
